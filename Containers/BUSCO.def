Bootstrap: docker

From: continuumio/miniconda3

%files
    BUSCO.yml

%post
    /opt/conda/bin/conda env create -f BUSCO.yml
    echo ". /opt/conda/etc/profile.d/conda.sh" >> $SINGULARITY_ENVIRONMENT
    echo "conda activate BUSCO" >> $SINGULARITY_ENVIRONMENT
    
    
%runscript
    busco --help